server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Simple proxy path to backend for demo
  location /api/health {
    proxy_pass http://host.docker.internal:8000/health;
  }
}
